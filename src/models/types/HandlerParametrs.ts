/* eslint-disable no-unused-vars */
type HandlerParametrs = {
  handlerLength: number;
  handlerMinTranslate: number;
  handlerMaxTranslate: number;
  handlerTop: number;
  handlerBottom: number;
};

export default HandlerParametrs;
