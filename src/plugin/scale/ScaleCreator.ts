class ScaleCreator {
  constructor(options: ScaleOptions) {}

  private createScale = (options: ScaleOptions) => {
    const { viewConnector } = options;
    const { scaleElements } = viewConnector;
		
		for (let i=2; i<)

  };
}
