import { describe, test, expect, beforeEach, jest } from "@jest/globals";
import {Plugin} from '../src/plugin/SliderPlugin'

beforeEach(() => {
  const slider = new Plugin();
});

describe('Plugin', () => {
	test('Should return new position for current Handler', () => {
		
	})
})